# Database
DATABASE_URL="postgresql://user:password@localhost:5432/loltrackr?schema=public"

# NextAuth
NEXTAUTH_SECRET=your-secret-here
NEXTAUTH_URL=http://localhost:3000

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Facebook OAuth
FACEBOOK_CLIENT_ID=your-facebook-client-id
FACEBOOK_CLIENT_SECRET=your-facebook-client-secret

# Twitter/X OAuth
TWITTER_CLIENT_ID=your-twitter-client-id
TWITTER_CLIENT_SECRET=your-twitter-client-secret

# Google Cloud Platform
GCP_PROJECT_ID=your-gcp-project-id
GCP_BUCKET_NAME=your-gcs-bucket-name
GOOGLE_APPLICATION_CREDENTIALS=./gcp-service-account.json

# ============================================
# LLM Configuration (Choose ONE option)
# ============================================

# Option 1: Use direct Ollama connection (recommended)
# Make sure Ollama is running: ollama serve
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1:8b

# Option 2: Use template-based generation (no LLM required)
# USE_TEMPLATES=true

# Option 3: Use separate TTS service (optional, for voice narration)
# TTS_SERVICE_URL=http://localhost:8001

# ============================================
# Notes:
# - If USE_TEMPLATES=true, no LLM is used (fast, reliable)
# - If USE_TEMPLATES is not set, app will try Ollama, then fall back to templates
# - TTS_SERVICE_URL is optional - if not set, videos won't have voice narration
# - OPENAI_API_KEY is no longer used for video processing
# ============================================
